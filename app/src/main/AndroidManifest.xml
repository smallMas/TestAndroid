<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestAndroid">
        <activity android:name=".users.activity.FriendActivity"
            android:theme="@style/Theme.TestAndroid.NoActionBar"></activity>
<!--        <activity-->
<!--            android:name=".menu.activity.MenuActivity"-->
<!--            android:label="@string/title_activity_menu"-->
<!--            android:theme="@style/Theme.TestAndroid.NoActionBar" />-->
<!--        <activity android:name=".Found.activity.FoundActivity" />-->
        <activity
            android:name=".menu.activity.MenuActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.TestAndroid.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>